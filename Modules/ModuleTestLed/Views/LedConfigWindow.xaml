<Window x:Class="ModuleTestLed.Views.LedConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="LED Configuration" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="CMD Control All (hex):" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtCmdAll" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="CMD Control Led (hex):" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtCmdLed" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Max Ports:" VerticalAlignment="Center" Margin="0,8"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,8">
            <TextBox x:Name="TxtMaxPorts" Width="60"
                     Style="{StaticResource MaterialDesignTextBox}"/>
            <Button Content="Refresh" Click="OnRefreshPorts" Margin="8,0,0,0"
                    Style="{StaticResource MaterialDesignFlatButton}" Padding="12,2"
                    ToolTip="Update per-port list to match new port count"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Max RGBW Value:" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtMaxRgbw" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="LEDs per Port:" FontWeight="SemiBold" Margin="0,12,0,4"/>

        <DataGrid Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                  x:Name="DgLedsPerPort"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  CanUserReorderColumns="False" CanUserSortColumns="False"
                  HeadersVisibility="Column"
                  Margin="0,4,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Port" Binding="{Binding Port}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Max LEDs (1-255)" Binding="{Binding MaxLeds}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Save" Click="OnSave" Margin="0,0,8,0"
                    Style="{StaticResource MaterialDesignRaisedButton}" Padding="24,6"/>
            <Button Content="Cancel" Click="OnCancel"
                    Style="{StaticResource MaterialDesignFlatButton}" Padding="24,6"/>
        </StackPanel>
    </Grid>
</Window>
