<Window x:Class="ModuleTestLed.Views.LedConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="LED Configuration" Height="400" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="CMD Control All (hex):" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtCmdAll" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="CMD Control Led (hex):" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtCmdLed" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Max Ports:" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtMaxPorts" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Max LEDs per Port (≤80):" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtMaxLeds" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Max RGBW Value:" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtMaxRgbw" Margin="0,8"
                 Style="{StaticResource MaterialDesignTextBox}"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Save" Click="OnSave" Margin="0,0,8,0"
                    Style="{StaticResource MaterialDesignRaisedButton}" Padding="24,6"/>
            <Button Content="Cancel" Click="OnCancel"
                    Style="{StaticResource MaterialDesignFlatButton}" Padding="24,6"/>
        </StackPanel>
    </Grid>
</Window>
