/*
 * Stub Packet32.h — satisfies the #include in nicdrv.h when building
 * soem_wrapper_compat.dll.  The compat shim only needs SOEM struct layouts;
 * it never calls any WinPcap / Npcap Packet API functions directly.
 */
#ifndef _PACKET32_H
#define _PACKET32_H
#endif
